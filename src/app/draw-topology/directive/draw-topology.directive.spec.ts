import { DrawTopologyDirective } from './draw-topology.directive';

describe('DrawTopologyDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawTopologyDirective(null);
    expect(directive).toBeTruthy();
  });
});
